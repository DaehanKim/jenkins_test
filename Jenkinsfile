pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'npm --version'
            }
        }
       
        stage('test') {
            steps {
                echo 'npm --version'
            }
        }
    }
}
